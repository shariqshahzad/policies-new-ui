import * as React from 'react';

interface {{pascalCase name}}Props {
  name?: string;
}

const {{pascalCase name}} = ({ name }: {{pascalCase name}}Props) => {
  return <>{name}</>;
};

export default {{pascalCase name}};
