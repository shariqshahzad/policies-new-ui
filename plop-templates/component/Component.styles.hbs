import styled from 'styled-components';

const ContainerWrapper = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: 100%;
`;

ContainerWrapper.displayName = 'ContainerWrapper-Wrapper';

export const Styled = {
  ContainerWrapper,
};
