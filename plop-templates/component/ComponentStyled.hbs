import React from 'react';

import { {{pascalCase name}}Props } from './{{pascalCase name}}.types';
import { Styled } from './{{pascalCase name}}.styles';

export const {{pascalCase name}} = ({}: {{pascalCase name}}Props) => {
  return (<Styled.ContainerWrapper>{{pascalCase name}}</Styled.ContainerWrapper>);
};