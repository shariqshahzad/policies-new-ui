import * as React from 'react';

export interface {{pascalCase name}}Props {
  name?: string;
}

export const {{camelCase name}} = () => {
  const [state] = React.useState<string>('1');
  return state;
};
